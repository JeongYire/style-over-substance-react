import React, { useRef, useState } from 'react';
import { BoxMode } from '../../../../types';

export default (params : {}) => {
  
    return (
        <div>
            <span>답변</span>
            <input disabled/>
        </div>
    );
};
